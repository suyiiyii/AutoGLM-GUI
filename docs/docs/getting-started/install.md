---
title: 安装与启动
---

本项目提供桌面版（Electron）和服务端运行两类方式。对普通用户来说，**桌面版是默认选择**。

## 桌面版（推荐）

桌面版启动后会自动打开界面。你只需要完成以下两件事：

1. 确保设备已连接（USB 或 WiFi）。
2. 在界面设置模型配置（基础模型与 API Key）。

## 服务端方式（高级用户）

如果你需要部署到服务器，可以使用 Docker 或命令行方式。详细说明见「部署」章节。

