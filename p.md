这个系统能不能分层 OpenAI sdk，基础agent（phoneagent，mai agent），编排模式（经典，双模型，分层） 模型的三层
同时设备通过 DeviceManager 获取，使用 通用的 Device 接口和基础agent 我的这个认知正确吗


既然各个之间是分层的，帮我找到不同层之间交互的接口（protocol），然后评估一下这个接口有没有作为当前层与层直接交互的唯一方式，以及这个接口的定义能不能满足当前的需求，现在打算对项目做一点重构，优化系统结构